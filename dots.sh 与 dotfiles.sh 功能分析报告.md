# dots.sh 与 dotfiles.sh 功能分析报告

## 总结

`dots.sh` 和 `dotfiles.sh` 这两个脚本在功能上有一定的重叠，但并不完全重复。它们服务于相似的目标（管理 dotfiles），但采用了不同的实现方式和设计理念，适用于不同的使用场景。

*   **`dotfiles.sh`**: 一个基于 `stow` 的手动管理工具集，提供了多个独立的 shell 函数，用于对单个配置模块进行初始化、链接、删除等精细化操作。它更灵活，适合逐步迁移和日常管理。
*   **`dots.sh`**: 一个自动化的批量处理入口脚本，通过调用其他脚本来完成两大核心功能：`backup`（备份）和 `deploy`（部署）。它旨在对一个预定义的配置文件列表进行一键式的完整备份或部署，自动化程度更高。

## 详细分析

### `dotfiles.sh`

*   **核心依赖**: `gnu-stow`
*   **设计理念**: 提供一组独立的、功能明确的 shell 函数，作为 `stow` 命令的封装，简化手动操作。
*   **主要功能**:
    *   `dotfiles-init <模块名>`: 将家目录下匹配的配置文件（如 `.zshrc`）移动到 dotfiles 仓库的相应模块目录中（如 `shell/`），然后使用 `stow` 创建符号链接。
    *   `dotfiles-rebuild <模块名>`: 使用 `stow` 重新为一个或多个模块创建符号链接。适用于在新机器上恢复配置或修复损坏的链接。
    *   `dotfiles-delete <模块名>`: 使用 `stow` 删除指定模块的符号链接。
    *   `dotfiles-count`: 统计家目录下尚未被符号链接管理的 dotfiles。
*   **适用场景**:
    *   当需要将一个新的配置文件纳入版本控制时。
    *   当只需要部署或更新某一个特定应用的配置时（例如，只更新 `git` 的配置）。
    *   需要对符号链接进行精细化、手动管理。

### `dots.sh`

*   **核心依赖**: `rsync`, `ln -s`, 以及 `scripts/` 目录下的辅助脚本。
*   **设计理念**: 提供一个统一的入口，通过简单的命令（`backup` 或 `deploy`）对所有预定义的配置文件进行批量操作。
*   **主要功能**:
    *   `backup`:
        *   执行 `scripts/backup.sh`。
        *   使用 `rsync` 将家目录下的实际配置文件**复制**到 dotfiles 仓库中，实现物理备份。
        *   这个过程是单向的：从 `$HOME` 目录 -> `dotfiles` 仓库。
        *   还会额外备份 `pacman` 的软件包列表。
    *   `deploy`:
        *   执行 `scripts/deploy.sh`。
        *   遍历一个在 `scripts/files.sh` 中定义的列表（`$TARGETS`）。
        *   使用 `ln -s` 命令在家目录下为列表中的每个文件**手动创建符号链接**，指向 dotfiles 仓库。
*   **适用场景**:
    *   在新机器上进行“一键式”环境部署。
    *   定期将本地的所有配置更改批量备份到 dotfiles 仓库。
    *   不希望使用 `stow`，而是采用更传统的 `ln -s` 循环脚本来管理链接。

## 功能重叠与差异对比

| 特性 | `dotfiles.sh` | `dots.sh` |
| :--- | :--- | :--- |
| **核心功能** | 符号链接管理 | 备份 & 部署 |
| **链接工具** | `stow` | `ln -s` |
| **操作粒度** | 精细化、按模块手动操作 | 批量化、对预定义列表自动操作 |
| **备份功能** | 无 | 有（使用 `rsync`） |
| **灵活性** | 高 | 低 |
| **自动化程度** | 低 | 高 |

## 结论

两个脚本可以共存，甚至互补。

*   你可以使用 `dotfiles.sh` 中的 `dotfiles-init` 函数来方便地将新的配置文件迁移到仓库中进行管理。
*   然后，你可以使用 `dots.sh backup` 来定期将这些文件的最新版本从家目录同步到仓库中。
*   最后，当你更换到一台新电脑时，可以使用 `dots.sh deploy` 来快速地将所有配置的符号链接一次性全部创建好。

因此，建议保留两个文件，并根据具体需求选择使用哪个脚本的哪个功能。
