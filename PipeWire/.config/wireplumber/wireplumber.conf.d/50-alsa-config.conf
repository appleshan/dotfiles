monitor.alsa.rules = [
  {
    matches = [
      {
        node.name = "~alsa_output.*"
      }
    ]
    actions = {
      update-props = {
        api.alsa.period-size   = 1024    # 周期缓冲区大小
        api.alsa.headroom      = 8192    # 预留缓冲空间
      }
    }
  }
]