######################################################################
#
#   ██╗██████╗ ██╗    ██╗███╗   ███╗
#   ██║╚════██╗██║    ██║████╗ ████║
#   ██║ █████╔╝██║ █╗ ██║██╔████╔██║
#   ██║ ╚═══██╗██║███╗██║██║╚██╔╝██║
#   ██║██████╔╝╚███╔███╔╝██║ ╚═╝ ██║
#   ╚═╝╚═════╝  ╚══╝╚══╝ ╚═╝     ╚═╝
#
#   Appleshan's i3 config
#   https://github.com/appleshan/dotfiles
#
#   Date    : 11/29/2019
#   Author  : Appleshan
#   Version : v1.1
#
#   依赖: picom polybar (可选推荐: dunst, rofi, feh)
#   部分特性可能需要新版本支持，请更新至最新版本或自行修改相应部分
#   更多内容请参考:
#   i3 User’s Guide:
#      http://i3wm.org/docs/userguide.html
#      https://github.com/zjuyk/i3wm-userguide-zh
#
######################################################################

# => 字体
# Font for window titles.
# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
font pango: DejaVu Sans Mono, FontAwesome 12

# => Super key ==  ⊞ Windows logo key  ==  Apple ⌘ Command key
set $super  Mod4

# Set default container layout for i3 (<default|stacking|tabbed>)
workspace_layout tabbed
# 自动切换到新打开的窗口 (smart|urgent|focus|none)
focus_on_window_activation smart

# 延迟工作区变化紧急提示重置
force_display_urgency_hint 2000 ms

# => 窗口边框类型（边框类型有 <normal(正常)|1pixel|pixel xx(自定义宽度)|none(无边框)|pixel>）
# 默认普通窗口的边框类型
new_window pixel 4
# 默认浮动窗口的边框类型
new_float normal
# 设置窗口边框隐藏 (none|vertical|horizontal|both|smart)
# hide_edge_borders both


###############################################################################
#  Application openners  
###############################################################################

# Open terminal (i3-sensible-terminal)
bindsym $super+Return  exec --no-startup-id  i3-sensible-terminal

# Start file manager (ranger、pcmanfm or thunar)
bindsym $super+F5  exec --no-startup-id  thunar

# Open web browser
bindsym $super+F3  exec --no-startup-id  google-chrome-stable

# flameshot 屏幕截图
bindsym Print  exec --no-startup-id  flameshot gui --delay 200

# flameshot config
bindsym $super+shift+Print  exec --no-startup-id  flameshot config

# soundcard mixer
bindsym $super+shift+Ctrl+m  exec --no-startup-id  alacritty -e 'alsamixer'


###############################################################################
#   Service Controls    
###############################################################################

# Rofi show applications
bindsym $super+d  exec --no-startup-id  rofi -show drun -columns 3

# Power Menu
# Repo: https://github.com/jluttine/rofi-power-menu
# No icons: https://github.com/jluttine/rofi-power-menu/issues/8#issuecomment-1440149437
bindsym $super+shift+p  exec --no-startup-id  $HOME/.config/polybar/scripts/system-power-menu/rofi-power-menu.sh

# Use rofi to search documentation on Zeal and Man
bindsym $super+g  exec --no-startup-id  $HOME/.config/rofi/scripts/rofi-zeal/rofi-zeal.sh

# 多媒体播放
bindsym $super+Ctrl+m  exec --no-startup-id  $HOME/.config/rofi/scripts/rofi-media/media.sh


###############################################################################
#   Misc keys          
###############################################################################

# Use rofi to search book on 'ebook' dir
bindsym $super+shift+b  exec --no-startup-id  $HOME/.config/rofi/scripts/rofi-book-selector/rofi-book-selector.sh

# xkill
# https://unix.stackexchange.com/questions/383065/execute-xkill-in-i3
bindsym --release $super+Ctrl+x  exec  xkill; exec notify-send 'Run xkill...'


###############################################################################
#   I3 conrols         
###############################################################################

# Rofi show window
bindsym $super+Tab  exec --no-startup-id  rofi -show window

# 当切换当前工作区时自动来回切换
workspace_auto_back_and_forth yes
# last activate workspace
bindsym $super+comma  workspace back_and_forth

# switch window in workspace
## 在窗口之间导航焦点 // Window // <Super> ↑ ↓ ← → ##
bindsym $super+Left   focus left
bindsym $super+Down   focus down
bindsym $super+Up     focus up
bindsym $super+Right  focus right

# 绑定了 Redo 按键，它实际对应的是原右 ALT 按键
bindsym Redo exec ~/.config/metaesc/restore_fullscreen.sh \
        && exec ~/.config/metaesc/focus_next_win.sh

# 移动窗口
# move focused window with cursor keys
## 移动聚焦的窗口 // Window // <Super><Shift> ↑ ↓ ← → ##
#bindsym $super+Shift+Left   move left
#bindsym $super+Shift+Down   move down
#bindsym $super+Shift+Up     move up
#bindsym $super+Shift+Right  move right

# 暂存窗口（额外的可隐藏浮动窗口，取消浮动还原成普通窗口）
## 转换普通窗口为暂存窗口 // Scratchpad // <Super><Shift> - ##
bindsym $super+Shift+minus  move scratchpad
## 呼出/隐藏暂存窗口 // Scratchpad // <Super> - ##
bindsym $super+minus  scratchpad show

# Resize panel 10x10 pixel
bindsym $super+Ctrl+shift+Left   resize shrink width 10 px or 10 ppt
bindsym $super+Ctrl+shift+Right  resize grow   width 10 px or 10 ppt
bindsym $super+Ctrl+shift+Up     resize shrink height 10 px or 10 ppt
bindsym $super+Ctrl+shift+Down   resize grow   height 10 px or 10 ppt

# Navigate workspaces next / previous
## 导航下一个/上一个工作区 // Window // <Super><Ctrl> ← → ##
bindsym $super+Ctrl+Left   workspace prev
bindsym $super+Ctrl+Right  workspace next

# => 布局切换 (change container layout <stacked, tabbed, toggle split>)

# Switch to stacked layout
## 切换到堆叠布局 // Window // <Super> s ##
#bindsym $super+s  layout stacking

# Switch to tabbed layout
## 切换到标签布局 // Window // <Super> w ##
#bindsym $super+w  layout tabbed

# Switch to tiled layout
## 切换到平铺布局（竖直/水平） // Window // <Super> e ##
#bindsym $super+e  layout toggle split

# Switch back and forth in all layouts <stacked, tabbed, toggle split>
## 在所有布局中轮回切换 // Window // <Super> x ##
#bindsym $super+x  layout toggle all

# enter fullscreen mode for the focused container
## 窗口切换到全屏 // Window // <Super> f ##
bindsym $super+f  fullscreen toggle

# => float window setting
# Use Mouse+$super to drag floating windows to their wanted position
floating_modifier $super
# toggle tiling / floating
## 窗口切换到浮动 // Window // <Super><Shift><space> ##
bindsym $super+Shift+space  floating toggle
# floating_maximum_size 1200 x 900
floating_maximum_size -1 x -1

# toggle sticky
## 窗口切换为粘滞 // Window // <Super><Shift> s ##
#bindsym $super+Shift+s  sticky toggle

# => 焦点切换
# 焦点跟随鼠标移动 (change focus with cursor)
focus_follows_mouse yes
# 焦点切换到父窗口 (focus the parent container)
# bindsym $super+q focus parent
# 焦点切换回子窗口 (focus the child container)
# bindsym $super+z focus child
# 焦点切换到浮动窗口 (change focus between tiling / floating windows)
## 焦点切换到浮动窗口 // Window // <Super><space> ##
bindsym $super+space  focus mode_toggle

# => 新建窗口的分割方向
# Split window horizontally
## 水平分割视窗 // Workspace // <Super><Ctrl> h ##
#bindsym $super+Ctrl+h  split h;exec notify-send 'tile horizontally'
# Split window vertically
## 垂直分割窗口 // Workspace // <Super><Ctrl> v ##
#bindsym $super+Ctrl+v  split v;exec notify-send 'tile vertically'

## 在三种边框类型中切换 // Workspace // <Super> b ##
bindsym $super+b border toggle
# change borders
# bindsym $super+u  border none
# bindsym $super+y  border pixel 1
# bindsym $super+n  border normal

# => 设定工作区名 (workspace names)
# icons assigned by `polybar`
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws0 "10"

# => 工作区切换&智能启动（添加智能启动脚本 ~/.local/bin/st 自动且不重复打开工作区相应程序）
## 转到工作区 // Workspace // <Super> 1...0 ##
bindsym $super+1  workspace $ws1, exec --no-startup-id ~/.local/bin/st alacritty
bindsym $super+2  workspace $ws2, exec --no-startup-id ~/.local/bin/st Telegram
bindsym $super+3  workspace $ws3, exec --no-startup-id ~/.local/bin/st google-chrome-stable
bindsym $super+4  workspace $ws4
bindsym $super+5  workspace $ws5, exec --no-startup-id ~/.local/bin/st thunar
bindsym $super+6  workspace $ws6
bindsym $super+7  workspace $ws7
bindsym $super+8  workspace $ws8
bindsym $super+9  workspace $ws9, exec --no-startup-id ~/.local/bin/st cc-switch
bindsym $super+0  workspace $ws0

# => 移动窗口到另一个工作区

# Move focused container to workspace
## 将聚焦的容器移动到另一个工作区 // Workspace // <Super><Ctrl> 1...0 ##
bindsym $super+Ctrl+1  move container to workspace $ws1
bindsym $super+Ctrl+2  move container to workspace $ws2
bindsym $super+Ctrl+3  move container to workspace $ws3
bindsym $super+Ctrl+4  move container to workspace $ws4
bindsym $super+Ctrl+5  move container to workspace $ws5
bindsym $super+Ctrl+6  move container to workspace $ws6
bindsym $super+Ctrl+7  move container to workspace $ws7
bindsym $super+Ctrl+8  move container to workspace $ws8
bindsym $super+Ctrl+9  move container to workspace $ws9
bindsym $super+Ctrl+0  move container to workspace $ws0

# Move to workspace with focused container
## 带上聚焦的容器移动到另一个工作区 // Workspace // <Super><Shift> 1...0 ##
bindsym $super+Shift+1  move container to workspace $ws1; workspace $ws1
bindsym $super+Shift+2  move container to workspace $ws2; workspace $ws2
bindsym $super+Shift+3  move container to workspace $ws3; workspace $ws3
bindsym $super+Shift+4  move container to workspace $ws4; workspace $ws4
bindsym $super+Shift+5  move container to workspace $ws5; workspace $ws5
bindsym $super+Shift+6  move container to workspace $ws6; workspace $ws6
bindsym $super+Shift+7  move container to workspace $ws7; workspace $ws7
bindsym $super+Shift+8  move container to workspace $ws8; workspace $ws8
bindsym $super+Shift+9  move container to workspace $ws9; workspace $ws9
bindsym $super+Shift+0  move container to workspace $ws0; workspace $ws0

# => 自动将窗口放到指定工作区（支持定义边框类型，窗口布局，大小调整，自动归类工作区等等，多个定义用,隔开）
# title 需要通过在终端执行 xprop，然后点击对应的 window, 
# 在显示信息中查找 _NET_WM_NAME 变量对应的名称，如果没有该变量，则匹配 WM_NAME 变量里的窗口名称
for_window [class="urxvt"] move container to workspace $ws1, workspace $ws1, layout tabbed
for_window [class="^org.wezfurlong.wezterm$"] move container to workspace $ws1, workspace $ws1, layout tabbed
for_window [class="^com.mitchellh.ghostty$"] move container to workspace $ws2, workspace $ws2, layout tabbed
for_window [class="^TelegramDesktop$"] move container to workspace $ws2, workspace $ws2, layout tabbed
for_window [class="Google-chrome"] move container to workspace $ws3, workspace $ws3, layout tabbed
for_window [title="uGet"] move container to workspace $ws3, workspace $ws3, layout tabbed
for_window [class="Thunderbird"] move container to workspace $ws3, workspace $ws3, layout tabbed
for_window [title="VLC media player"] move container to workspace $ws4, workspace $ws4, layout tabbed
#for_window [class="File Manager PCManFM"] move container to workspace $ws5; workspace $ws5, layout tabbed
for_window [class="Thunar"] move container to workspace $ws5; workspace $ws5, layout tabbed
for_window [class="Sublime"] move container to workspace $ws6; workspace $ws6, layout tabbed
for_window [class="Zeal"] move container to workspace $ws6; workspace $ws6, layout tabbed
for_window [class="Code"] move container to workspace $ws7; workspace $ws7, layout tabbed
for_window [class="Emacs"] move container to workspace $ws7; workspace $ws7, layout tabbed
for_window [class="cc-switch"] move container to workspace $ws9; workspace $ws9
for_window [class="org.remmina.Remmina"] move container to workspace $ws0; workspace $ws0
for_window [class="Gnome-boxes"] move container to workspace $ws0; workspace $ws0
for_window [class="VirtualBox Manager"] move container to workspace $ws0; workspace $ws0
for_window [class="v2rayn"] move container to workspace $ws0, workspace $ws0, layout tabbed

for_window [class="Arandr"] floating enable
for_window [class="Nm-connection-editor"] floating enable
for_window [class="Xfce4-power-manager-settings"] floating enable

# floating pop up automatically
for_window [window_role="pop-up"] floating enable
for_window [window_role="task_dialog"] floating enable

# => i3-gaps

# Set inner/outer gaps
set $default-gaps-inner 5
set $default-gaps-outer 5
gaps inner $default-gaps-inner
gaps outer $default-gaps-outer

# Smart gaps (gaps used if only more than one container on the workspace)
# smart_gaps on

# Smart borders (draw borders around container only if it is not the only container on this workspace)
# on|no_gaps (on=always activate and no_gaps=only activate if the gap size to the edge of the screen is 0)
smart_borders on

# adjust gaps on-the-fly
# Press $super+Shift+g to enter the gap mode.
# Choose o or i for modifying outer/inner gaps.
# Press one of + / - (in-/decrement for current workspace)
# or 0 (remove gaps for current workspace).
# If you also press Shift with these keys, the change will be global for all workspaces.
set $mode-gaps "Gaps: (o) outer, (i) inner"
set $mode-gaps-outer "Outer Gaps: +|-|0 (local), Shift + +|-|0 (global)"
set $mode-gaps-inner "Inner Gaps: +|-|0 (local), Shift + +|-|0 (global)"

mode $mode-gaps {
    bindsym o      mode $mode-gaps-outer
    bindsym i      mode $mode-gaps-inner

    bindsym 0      mode "default", exec --no-startup-id i3-msg "gaps inner current set 0" && i3-msg "gaps outer current set 0"
    bindsym d      mode "default", exec --no-startup-id i3-msg "gaps inner current set $default-gaps-inner" && i3-msg "gaps outer current set $default-gaps-outer"

    bindsym Escape mode default
}

mode $mode-gaps-inner {
    bindsym plus  gaps inner current plus 1
    bindsym minus gaps inner current minus 1
    bindsym 0     mode "default", gaps inner current set 0
    bindsym d     mode "default", gaps inner current set $default-gaps-inner

    bindsym Shift+plus  gaps inner all plus 1
    bindsym Shift+minus gaps inner all minus 1
    bindsym Shift+0     mode "default", gaps inner all set 0
    bindsym Shift+d     mode "default", gaps inner all set $default-gaps-inner

    bindsym Escape mode default
}

mode $mode-gaps-outer {
    bindsym plus  gaps outer current plus 1
    bindsym minus gaps outer current minus 1
    bindsym 0     mode "default", gaps outer current set 0
    bindsym d     mode "default", gaps outer current set $default-gaps-outer

    bindsym Shift+plus  gaps outer all plus 1
    bindsym Shift+minus gaps outer all minus 1
    bindsym Shift+0     mode "default", gaps outer all set 0
    bindsym Shift+d     mode "default", gaps outer all set $default-gaps-outer

    bindsym Escape mode default
}

# => kill focused window
## 关闭聚焦的窗口 // Window // <Super> q ##
set $close "Close the window? (y/n)"
mode $close {
    bindsym y kill, mode default
    bindsym Return kill, mode default
    bindsym q kill, mode default
    bindsym n mode default
    bindsym Escape mode default
}
bindsym $super+q mode $close

# => 调整窗口大小 (resize window with hjkl)
set $resize " resize h=shrink_5 j=grow_5 k=shrink_5 l=grow_5"
mode $resize {
    # these bindings trigger as soon as you enter the resize mode

    # pressing 'LEFT' or 'H' will shrink the window’s width.
    # pressing 'RIGHT' or 'L' will grow the window’s width.
    # pressing 'UP' or 'K' will shrink the window’s height.
    # pressing 'DOWN' or 'J' will grow the window’s height.
    bindsym Down   resize grow height 5 px or 5 ppt
    bindsym Up     resize shrink height 5 px or 5 ppt
    bindsym Right  resize grow width 5 px or 5 ppt
    bindsym Left   resize shrink width 5 px or 5 ppt
    bindsym j      resize grow height 5 px or 5 ppt
    bindsym k      resize shrink height 5 px or 5 ppt
    bindsym l      resize grow width 5 px or 5 ppt
    bindsym h      resize shrink width 5 px or 5 ppt

    bindsym 7 resize set 50 ppt 50 ppt
    bindsym 8 resize set 53 ppt 47 ppt
    bindsym 9 resize set 62 ppt 38 ppt

    # back to normal: Escape
    bindsym Escape mode default
}

# => 交换窗口 (swap window with shift hjkl)
set $swap-window "swap h=left j=down k=up l=right"
mode $swap-window {
    bindsym h move left
    bindsym j move down
    bindsym k move up
    bindsym l move right

    # back to normal: Escape
    bindsym Escape mode default
}

# => 修改工作区布局的子菜单
set $layout-mode "layout stack[s] tabbed[w] split[e] other toggle[o]"
mode $layout-mode {
    # change container layout (stacked, tabbed, toggle split)
    bindsym s layout stacking
    bindsym w layout tabbed
    bindsym e layout toggle split
    bindsym o layout toggle all

    # bindsym p focus parent
    # bindsym c focus child

    # 用 \ 切换左右布局
    bindsym backslash split h; exec notify-send " | split"
    # 用 - 切换为上下布局
    bindsym minus split v; exec notify-send "-- split"

    # back to normal: Escape
    bindsym Escape mode default
}

# => 多屏切换
# New mode for switching monitor modes
set $monitorMode "monitor s=single, d=dual-screen"
mode $monitorMode {
    bindsym s  exec "~/.config/i3/screenlayout/single"
    bindsym d  exec "~/.config/i3/screenlayout/dual-screen"

    # back to normal: Escape
    bindsym Escape  mode default
}

set $volume-mode "Volume: Mute(F1), Volume-(F2), Volume+(F3), MicMute(F4)"
mode $volume-mode {
    bindsym F1 exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $audio-note
    bindsym F2 exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -2% && $audio-note
    bindsym F3 exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +2% && $audio-note
    bindsym F4 exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $audio-note

    # back to normal: Escape
    bindsym Escape mode default
}

# => 切换键盘布局的子菜单（binding mode）
exec_always --no-startup-id exec ~/.config/metaesc/code01s2.sh

set $keymap-switch-mode "keymap: s(2), metaesc(1), default(0)"
mode $keymap-switch-mode {
    bindsym 0 exec ~/.config/metaesc/code01default.sh, mode default
    bindsym 1 exec ~/.config/metaesc/code01s1.sh, mode default
    bindsym 2 exec ~/.config/metaesc/code01s2.sh, mode default

    # back to normal: Escape
    bindsym Escape mode default
}

set $main-mode "Menu swap/resize[hjkl,7,8,9] gaps[g] keymaps[k] layout[l] monitor[m] volume[v]"
mode $main-mode {
    bindsym g  mode $mode-gaps
    bindsym k  mode $keymap-switch-mode
    bindsym l  mode $layout-mode
    bindsym m  mode $monitorMode
    bindsym r  mode $resize
    bindsym s  mode $swap-window
    bindsym v  mode $volume-mode

    # => reload the configuration file（更改配置文件后只须重载即可生效，不包含自启部分）
    ## 重新载入 i3 配置 // i3 WM // <Super><Shift> c ##
    bindsym Shift+c  reload; exec notify-send 'Reload i3wm config'

    # restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
    ## 重启 i3 窗口管理器 // i3 WM // <Super><Shift> r ##
    bindsym Shift+r  restart; exec notify-send 'Restart i3wm'

    # back to normal: Escape
    bindsym Escape mode default
}

bindsym $super+semicolon  mode $main-mode

# => startup apps and daemons

# • exec命令
#   • exec 执行命令
#   • --no-startup-id 有些脚本或者程序不支持启动通知，不加命令，鼠标会长时间空转，60秒左右
#   • exec_always 每次重启i3，使用该命令启动的程序都会重新执行一次，不然只在开机时执行一次。

# `polybar` replaces `i3bar`
# more info at https://github.com/jaagr/polybar
# configuration in ~/.config/polybar/config.ini
exec_always --no-startup-id ~/.config/polybar/launch.sh

# => Copy/Paste 的统一
bindsym F8 --release exec ~/.config/metaesc/paste.sh c
bindsym F9 --release exec ~/.config/metaesc/paste.sh v

# => piper-speak
# https://github.com/appleshan/piper-speak
bindsym $super+Shift+s  exec --no-startup-id  speak-selection

set $audio-note ~/.config/metaesc/show_audio.sh
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +2% && $audio-note
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -2% && $audio-note
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $audio-note
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $audio-note

set $bright-note ~/.config/metaesc/brightness_code01.sh
bindsym XF86MonBrightnessUp exec $bright-note 20
bindsym XF86MonBrightnessDown exec $bright-note -20

# enable syntax highlighting in vim/neovim. must have the i3 plugin!
# get it at https://github.com/PotatoesMaster/i3-vim-syntax
# vim:filetype=i3
