[Unit]
Description=tmux session
Documentation=man:tmux(1)

[Service]
Type=forking
Restart=always
# 让 start-server 和关闭 exit-empty 选项在同一个命令序列中执行.
ExecStart=tmux start-server \; set-option -g exit-empty off
ExecStop=/usr/bin/tmux kill-session -t default

[Install]
WantedBy=default.target
